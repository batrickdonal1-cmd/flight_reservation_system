# flight_reservation_system
 Flight Reservation System A simple Python project built using **Pandas** and *SQLite* to simulate an airline ticket booking system.  Features -  View available flights - Search flights by source and destination -  Book tickets and calculate total fare - 💾 Data stored in SQLite database - 📊 Flight data loaded from a CSV file (Kaggle dataset) 
